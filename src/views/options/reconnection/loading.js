import {SContainer} from '../../signin/styled'

const Loading = () => {
    return(
            <SContainer>
              <h2 className="waiting"> Aguarde! Estamos verificando...</h2>
          </SContainer>
    )
}
export default Loading
